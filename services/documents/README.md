# Documents Service

Deploys the SpendPilot document and AI surface:

- `/health`
- `/ready`
- `/api/documents/*`
- `/api/ai/*`

This service shares the common SpendPilot package in the repo root and is built with `services/documents/Dockerfile`.
